<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tain.mappers.TimeMapper">

	<select id="getTimeXML" resultType="string">
	<![CDATA[
		select now() as now2
	]]>
	</select>
	
</mapper>
